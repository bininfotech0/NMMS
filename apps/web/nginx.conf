server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Let React Router handle client-side routes such as /join after a direct
    # browser request or refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
